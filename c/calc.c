#include <stdio.h>

int main()
{
  int num, _num, __num, result;

  printf("Digite os valores: ");
  scanf("%d%d%d", &num, &_num, &__num);


  printf("Sum: %d\n",result = (num + _num + __num));

  return 0;
}
